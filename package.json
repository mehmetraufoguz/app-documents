{
  "name": "app-documents-monorepo",
  "private": true,
  "description": "Document management and publishing platform with SDK",
  "scripts": {
    "dev": "pnpm -C server dev",
    "build": "pnpm -C server build && pnpm -C client-sdk build",
    "preview": "pnpm -C server preview"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "lightningcss",
      "better-sqlite3"
    ]
  }
}
